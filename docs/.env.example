# ===========================================
# Spin Documentation Environment Variables
# ===========================================

# -----------------------------------------
# Local Development (default values work)
# -----------------------------------------
# For local development, you typically don't need to set any variables.
# The defaults assume the app runs at the root (http://localhost:3000/).

# -----------------------------------------
# Production Configuration
# -----------------------------------------
# When deploying to a subdirectory (e.g., https://serversideup.net/open-source/spin/),
# you must set these variables:

# NUXT_APP_BASE_URL
# The base URL path for Nuxt router (affects all route generation).
# Set this to the subdirectory path where the app is hosted.
# Example: /open-source/spin
# Default: / (root)
NUXT_APP_BASE_URL=

# NUXT_PUBLIC_BASE_URL
# The base URL path prepended to static assets (images, etc.).
# This should match NUXT_APP_BASE_URL for subdirectory deployments.
# Used by the useAssetUrl() composable for resolving image paths.
# Example: /open-source/spin
# Default: (empty string, assets served from root)
NUXT_PUBLIC_BASE_URL=

# TOP_LEVEL_DOMAIN
# The top-level domain used for SEO meta tags and canonical URLs.
# Example: https://serversideup.net
# Default: https://serversideup.net
TOP_LEVEL_DOMAIN=https://serversideup.net

# SITE_URL
# The full site URL used for sitemap and other site-wide configurations.
# Example: https://serversideup.net
# Default: https://serversideup.net
SITE_URL=https://serversideup.net

# ===========================================
# Example Configurations
# ===========================================

# Local Development:
# (No variables needed - defaults work)

# Production at https://serversideup.net/open-source/spin/:
# NUXT_APP_BASE_URL=/open-source/spin
# NUXT_PUBLIC_BASE_URL=/open-source/spin
# TOP_LEVEL_DOMAIN=https://serversideup.net
# SITE_URL=https://serversideup.net
