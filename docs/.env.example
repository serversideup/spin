# Spin Documentation (Static Site)
#
# Local dev:          No config needed (defaults to http://localhost:3000)
# CloudFlare preview: No config needed (auto-detects CF_PAGES_URL)
# Production:         Set SITE_URL to https://serversideup.net/open-source/spin

# Full canonical URL including subdirectory path (if any)
# Used for: routing, sitemap, SEO meta tags, asset paths
SITE_URL=
